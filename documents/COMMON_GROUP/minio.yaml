# minio 文件存储
minio:
  endpoint: ${MINIO_ENDPOINT:http://192.168.2.250:9090} # minio服务所在的地址
  bucketName: ${MINIO_BUCKETNAME:shanjupay} # 存储桶名称
  accessKey: ${MINIO_ACCESSKEY:minioadmin}
  secretKey: ${MINIO_SECRETKEY:minioadmin}